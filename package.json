{
  "name": "foldrun-brand",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The brand kit \u2014 the mark, the words, and the cover images for every profile. Private by convention: published, never indexed.",
  "scripts": {
    "dev": "astro dev --port 4340",
    "build": "npm run covers && npm run letterhead && astro build",
    "preview": "astro preview --port 4340",
    "covers": "node scripts/render.mjs",
    "letterhead": "node scripts/letterhead.mjs"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
